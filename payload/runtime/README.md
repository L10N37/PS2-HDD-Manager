# Runtime payload cache

The GUI populates this directory on demand through `fetch_payloads.sh`. Third-party binary payloads
are intentionally not distributed in the source archive; the selected current releases are downloaded
and validated before a physical format begins.
